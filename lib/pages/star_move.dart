import 'package:flutter/material.dart';

class StarMove extends StatefulWidget {
  const StarMove({Key? key}) : super(key: key);

  @override
  _StarMove createState() => _StarMove();
}

class _StarMove extends State<StarMove> {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.green,
      ),
    );
  }
}
